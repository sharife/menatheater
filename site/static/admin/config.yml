backend:
  name: git-gateway
  branch: main

media_folder: "site/static/img" # Folder where user uploaded files should go
public_folder: "img"

collections: # A list of collections the CMS should be able to edit
  - name: "announcement" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "Announcement" # Used in the UI, ie.: "New Post"
    folder: "site/content/announcements" # The path to the folder where the documents are stored
    path: "{{slug}}/index" # Each document is inside a folder with the slug name.
    create: true # Allow users to create new documents in this collection
    fields: # The fields each document in this collection have
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "text"}
      - {label: "Body", name: "body", widget: "markdown"}
      - label: "Photo Gallery"
        name: "photos"
        widget: "object"
        required: false
        fields: 
          - label: Heading
            name: heading
            widget: string
          - label: Photos
            name: items
            widget: list
            fields:
              - label: Image
                name: image
                widget: image
  - name: "convening" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "Convening" # Used in the UI, ie.: "New Post"
    folder: "site/content/convenings" # The path to the folder where the documents are stored
    path: "{{slug}}/index" # Each document is inside a folder with the slug name.
    create: true # Allow users to create new documents in this collection
    fields: # The fields each document in this collection have
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "text"}
      - {label: "Body", name: "body", widget: "markdown"}
      - {label: "Image", name: "image", widget: "image"}
  - name: "pages"
    label: "Pages"
    files:
      - file: "site/content/_index.md"
        label: "Home Page"
        name: "home"
        fields:
          - {label: Title, name: title, widget: string}
          - {label: Heading, name: heading, widget: string}
          - {label: Intro, name: intro, widget: text}
          - {label: "Slideshow", name: slideshow, widget: object, fields: [
              {label: "Image", name: "image", widget: "image"}]}
      - file: "site/content/contact/_index.md"
        label: "Contact Page"
        name: "contact"
        fields:
          - {label: Title, name: title, widget: string}
          - {label: Body, name: body, widget: markdown}
      - file: "site/content/grants/_index.md"
        label: "Grants Page"
        name: "grants"
        fields:
          - {label: Title, name: title, widget: string}
          - {label: Intro, name: intro, widget: text}
          - {label: Body, name: body, widget: markdown}
      - file: "site/content/resources/_index.md"
        label: "Resources Page"
        name: "resources"
        fields:
          - {label: Title, name: title, widget: string}
          - {label: Intro, name: intro, widget: text}
          - {label: Body, name: body, widget: markdown}
          - label: Organizations
            name: orgs
            widget: object
            fields:
              - {label: Heading, name: heading, widget: string}
              - label: Org
                name: items
                widget: list
                fields:
                  - {label: Link, name: link, widget: string}
                  - {label: Text, name: text, widget: markdown}
                  - {label: Image, name: image, widget: image}
  - name: "site_settings"
    label: "Site Settings"
    files:
      - file: "site/data/footer.json"
        label: Footer
        name: footer
        fields:
          - {label: Title, name: title, widget: string}
          - {label: Blurb, name: blurb, widget: text}
      - file: "site/hugo.toml"
        label: Hugo Config
        name: config
        editor:
          preview: false
        fields:
          - {label: Site Title for Browser Tab, name: title, widget: string}
          - label: Params
            name: params
            widget: object
            fields:
              - label: Open Graph Image
                name: images
                widget: list
                field: { label: Image, name: image, widget: image }
